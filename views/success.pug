
link(rel="stylesheet", href="css/success.css")

h2.notice Note: This link will be expired after 7 days and your file will be deleted.
h3.share-file You can share this link with others to download the file (click COPY to copy the link)

.input-group
    input.form-control.shared-link(type='text' value=`${protocol}://${download_url}` readOnly)
    span#copy.input-group-text COPY
div#msg
div.btn-container
    div#btn-continue-share 
        a(href=`${back_url}`) Continue to share

script(src="js/success.js")
        